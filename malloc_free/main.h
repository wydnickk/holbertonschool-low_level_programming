#ifndef MAIN_H
#define MAIN_H

/**
 * create_array - creates and initializes char array
 * @size: array size
 * @c: initial value
 * Return: pointer to array or NULL
 */
char *create_array(unsigned int size, char c);

#endif
